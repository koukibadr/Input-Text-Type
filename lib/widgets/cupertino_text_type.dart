import 'package:flutter/material.dart';

class CupertinoTextType extends StatelessWidget {
  const CupertinoTextType({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}